import { BtnLoadingRenderDirective } from './btn-loading-render.directive';

describe('BtnLoadingRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnLoadingRenderDirective();
    expect(directive).toBeTruthy();
  });
});
