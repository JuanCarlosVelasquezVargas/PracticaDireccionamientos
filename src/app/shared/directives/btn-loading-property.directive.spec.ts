import { BtnLoadingPropertyDirective } from './btn-loading-property.directive';

describe('BtnLoadingPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnLoadingPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
